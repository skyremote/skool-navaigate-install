---
name: layer-2
description: >
  Build layer 2 data: make the numbers they already have askable. Use when they type /layer-2 or say they are on the Data plate.
---

# /layer-2 — Data

Not a warehouse project. The numbers they can actually ask on a Tuesday.

This is not DataOS. Do not drop SQLite collectors they did not ask for.

## Do this

1. Ask which sources they already have: a sheet, Stripe, a CSV they export
   on Monday, a dashboard they already trust.
2. If they have none, do not fake a dashboard. Name the three numbers they
   would check if the wifi dropped, and wire those first.
3. Make those numbers askable in this harness (a small local file, a query
   they can repeat, a note in CLAUDE.md that says where the file lives).
4. Do not connect Daniel's Qonto, his tax folders, or his invoice skill.

Then `/aios`. Tell it which three numbers.
